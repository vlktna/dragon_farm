package com.dragon.farm.coursework.data.enum

enum class ActionType(val typeName: String) {
    FEED("feed"),
    PLAY("play"),
    TRAIN("train"),
    SCOLD("scold"),
    HIT("hit"),
    TREAT("treat")
}